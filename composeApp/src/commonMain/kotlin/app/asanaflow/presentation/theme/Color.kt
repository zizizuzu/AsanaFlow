package app.asanaflow.presentation.theme

import androidx.compose.ui.graphics.Color

// Yoga and meditation app colors extracted from screenshots
// Primary: Deep Purple (main headers and accents)
val Primary = Color(0xFF673AB7)

// Secondary: Teal (used for secondary accents)
val Secondary = Color(0xFF009688)

// Tertiary: Light Green (used for yoga-related elements)
val Tertiary = Color(0xFF8BC34A)

// Background colors
val Background = Color(0xFFFFFFFF) // Pure white as in screenshots
val Surface = Color(0xFFF5F5F5) // Light gray for cards/surfaces

// Text colors
val OnPrimary = Color(0xFFFFFFFF) // White text on colored backgrounds
val OnSecondary = Color(0xFFFFFFFF)
val OnTertiary = Color(0xFF000000)
val OnBackground = Color(0xFF000000) // Black text on white background
val OnSurface = Color(0xFF000000)

// Mood tracking colors (from "Underground" section)
val Unhappy = Color(0xFFE53935) // Red
val Sad = Color(0xFF7E57C2) // Purple
val Normal = Color(0xFF42A5F5) // Blue
val Good = Color(0xFF66BB6A) // Green
val Happy = Color(0xFFFFEE58) // Yellow

// Checkbox/UI elements (from todo list)
val CheckboxUnchecked = Color(0xFFBDBDBD)
val CheckboxChecked = Primary

// Dark theme colors (adapted from light scheme)
val DarkPrimary = Color(0xFF9575CD) // Lighter purple
val DarkSecondary = Color(0xFF4DB6AC) // Brighter teal
val DarkTertiary = Color(0xFFAED581) // Lighter green
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkOnPrimary = Color(0xFF000000)
val DarkOnSecondary = Color(0xFF000000)
val DarkOnTertiary = Color(0xFF000000)
val DarkOnBackground = Color(0xFFFFFFFF)
val DarkOnSurface = Color(0xFFFFFFFF)